# Modelos e pesos

- `detection/yolo/pretrained/`: checkpoints externos usados para inicialização;
- modelos treinados permanecem dentro da run que os produziu;
- uma promoção deve ser registrada por referência, sem duplicar o checkpoint.
